# Server Configuration
HOST=0.0.0.0
PORT=8181
DNS_PORT=5353  # Use 53 for production DNS (requires root or capabilities)

# Database
DATABASE_URL=sqlite:ddns.db

# Admin Credentials
ADMIN_PASSWORD=your_secure_password_here

# Domain Configuration
BASE_DOMAIN=ash-api.online

# Session Secret (must be at least 64 characters)
SESSION_SECRET=your_very_long_secret_key_at_least_64_characters_long_for_security
