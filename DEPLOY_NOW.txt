URGENT: Deploy this nginx config to your server NOW
=====================================================

The nginx config is fixed locally but NOT deployed to your server yet.

ON YOUR SERVER (178.128.26.139), run these commands:

1. Edit the nginx config:
   sudo nano /etc/nginx/sites-available/ash-api.online

2. Replace the HTTP server block (the one with "listen 80;") with this:

server {
    listen 80;
    listen [::]:80;
    server_name ash-api.online *.ash-api.online;

    # Logging
    access_log /var/log/nginx/ddns-http-access.log;
    error_log /var/log/nginx/ddns-http-error.log;

    # Allow Let's Encrypt ACME challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # DDNS update endpoints - MUST allow HTTP
    location /update {
        proxy_pass http://127.0.0.1:8181;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }

    location /nic/update {
        proxy_pass http://127.0.0.1:8181;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
    }

    # Redirect endpoint - allow HTTP
    location /redirect {
        proxy_pass http://127.0.0.1:8181;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Redirect all other traffic to HTTPS
    location / {
        return 301 https://$host$request_uri;
    }
}

3. Test the config:
   sudo nginx -t

4. If test passes, reload:
   sudo systemctl reload nginx

5. Test the endpoint:
   curl -v -u i5nqisufpxxm:q1HAwwWkvWD0oxs7 "http://ash-api.online/update?hostname=home.ash-api.online&myip=100.135.120.102"

Expected result: "good 100.135.120.102" (not 404)
